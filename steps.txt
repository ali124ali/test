1. create a directory with 'mkdir ...'
2. change directory to new one 'cd ...'
3. initialized git directory 'git init'
4. set local username just for this git repository 'git config --local user.name "..." ' 
5. set local email just for this git repository 'git config --local user.email "..." '
6. create new file 'touch ...'
7. check repository status 'git status'
8. staging all recent changes 'git add -A'
9. commit all staging changes 'git commit -m "..." '
10. for see the difference between current status and one commit ago for just a file 'git diff HEAD^ file.txt '
1k1.  for see the difference between current status and one commit ago for total git repository 'git diff HEAD^ '
just work with git new features ...


now i am learning git useful features
